# SACT 终稿设计定义与证明报告

## 1. 文档目标与范围

本文是当前 SACT 版本的统一定义文档，面向“实现可落地 + 理论可解释 + 汇报可复用”三个目标。

本文覆盖内容：

1. 当前编码格式与符号化定义。
2. 五阶段设计（A/B/C/D/E）的形式化目标。
3. 每个阶段的具体计算方法（不是只给概念）。
4. 与原始 Tree+VB 方案的差异与提升来源。
5. 统一优化目标、复杂性性质、NP-Hard 证明与下界证明。
6. 终稿版本的实测收益总结与最终结论。

---

## 2. 基础对象与编码格式定义

### 2.1 图与邻接表示

设图为 $G=(V,E)$。每个节点 $v$ 的邻接集合记为 $L_v$，在工程中以 CSR 形式存储：

$$
\text{CSR}=(row\_ptr, col\_idx)
$$

其中 $row\_ptr[v]..row\_ptr[v+1]-1$ 对应 $L_v$。

### 2.2 记录类型

当前终稿只使用两类记录。

1. single 记录：仅编码一个节点。
2. path 记录：编码一个底节点和向上回溯步数。

### 2.3 比特位格式

先写一个整数

$$
val=(bottom\ll 1)\,|\,flag
$$

约定：

1. $flag=0$ 表示 single。
2. $flag=1$ 表示 path。
3. $bottom=val\gg 1$。
4. 若 $flag=1$，后续再写一个 $steps$。

### 2.4 变长字节代价函数

记 $\ell(x)$ 为整数 $x$ 的 VByte 字节长度。

1. single 记录代价：

$$
C_{single}(u)=\ell((u\ll 1)|0)
$$

2. path 记录代价：

$$
C_{path}(bottom,steps)=\ell((bottom\ll 1)|1)+\ell(steps)
$$

说明：内存中的节点与索引是 32 位整型；压缩流是 VByte 变长字节流，不是定长 32/64 位。

---

## 3. 统一优化目标

定义全 single 基线代价：

$$
C_{base}=\sum_{(v,u)\in E} C_{single}(u)
$$

定义片段节约收益：

$$
W(s)=\sum_{u\in s} C_{single}(u)-C(s)
$$

则总目标可写为：

$$
\min C_{total}=C_{base}-S + C_{dict},\quad
S=\sum_{v\in V}\sum_{s\in\mathcal{S}_v}W(s)x_{s,v}
$$

其中：

1. $x_{s,v}\in\{0,1\}$ 是是否选择片段 $s$ 的决策变量。
2. 约束是“每行精确覆盖、片段互斥、父森林可达”。

直观上，问题等价于“最大化总节约量 $S$”。

### 3.1 Ratio(%) 口径定义

与 benchmark 表格保持一致：

$$
Ratio(\%) = \frac{BPE}{32} \times 100
$$

因此：

1. 优化上最小化 BPE 与最小化 Ratio(%) 等价（线性同比缩放）。
2. 汇报上需同时展示 BPE 与 Ratio(%)，便于和传统压缩比口径对齐。

---

## 4. 五阶段设计（定义 + 计算 + 对比）

## 4.1 阶段 A：双遍流式加载与 CSR 压紧

### 定义

阶段 A 目标是降低构图峰值内存，同时不改变图语义。

### 具体计算方式

1. 第一遍扫描输入边文件，统计：
   1. 最大节点编号。
   2. 每个节点度数 $deg(u)$。
2. 由 $deg$ 前缀和一次性构建 CSR 空间。
3. 第二遍扫描，把边流式写入 $col\_idx$。
4. 对每行排序去重，得到压紧后的最终 CSR。

### 相对原始方案的不同

原始方案是“全量边列表 + 每行临时 vector 邻接再合并”。

### 提升机制

1. 峰值内存从“边列表 + 邻接容器叠加”降到“CSR 主体为主”。
2. 为大图稳态执行提供基础。
3. 压缩率语义不变，等价性更强。

---

## 4.2 阶段 B：码长感知重编号

### 定义

阶段 B 目标是让高贡献节点优先分配短 VByte 编码区间。

### 具体计算方式

先统计三个量：

1. $f(u)$：节点出现频次（全边扫描计数）。
2. $p(u)$：在“按频次排序后的行相邻对”中，作为前驱节点的次数。
3. $b(u)$：在“按频次排序后的行相邻对”中，作为后继节点的次数。

定义分数：

$$
score(u)=\alpha f(u)+\beta p(u)+\gamma b(u)
$$

当前终稿参数：

$$
\alpha=1.00,\quad \beta=0.75,\quad \gamma=0.50
$$

再按 $score$ 降序分配新 ID（高分节点拿小 ID），并把 $col\_idx$ 全量映射到新 ID。

### 相对原始方案的不同

原始 Tree+VB 不做全局重编号，默认沿原始节点 ID。

### 提升机制

1. 直接优化 VByte 主导成本。
2. 在不改解码语义下，改变“同样片段对应的字节长度”。
3. 实测对 BPE 是稳定增益来源。

---

## 4.3 阶段 C：收益驱动父森林（保守覆盖）

### 定义

阶段 C 目标是把“父边选择”从纯 first-fit 改为“收益驱动 + 置信门控”。

### 具体计算方式

对每个局部父子候选 $p\to c$：

1. 单点代价：

$$
single\_cost=C_{single}(c)+C_{single}(p)
$$

2. 一步路径代价：

$$
path\_cost=C_{path}(c,1)
$$

3. 单位收益：

$$
unit\_gain=single\_cost-path\_cost
$$

4. 稳定性项：

$$
stability=\frac{f(p)}{\max(f(c),1)}
$$

5. 轻量码长惩罚：

$$
id\_penalty=\ell((c\ll 1)|1)-1
$$

6. 局部分数：

$$
local\_score=unit\_gain+0.20\cdot stability-0.05\cdot id\_penalty
$$

7. 每个子节点保留 Top-K 候选（当前 $K=4$），累计 score 与 support。
8. 默认先保留 first-fit 父边，再在高置信条件下覆盖：

$$
support\ge 2\ \text{且}\ score\ge 2.0
$$

### 相对原始方案的不同

原始 Tree+VB 只做 first-fit，谁先出现就绑定谁。

### 提升机制

1. 父关系更接近真实字节收益最大化。
2. 阈值回退保证稳定性，避免激进误绑导致退化。

---

## 4.4 阶段 D：边际收益截断与最优前缀

### 定义

阶段 D 目标是避免“路径越长越好”的错误，改为“收益最优前缀”。

### 具体计算方式

对每个候选底节点 $bottom$，沿父链扩展第 $t$ 步时定义：

$$
S_t=Bytes_{single}(prefix_t)-Bytes_{path}(bottom,t)
$$

工程中用增量方式维护：

1. 每延伸一步更新 single\_cost。
2. 实时计算 path\_cost。
3. gain = single\_cost - path\_cost。
4. 记录 best\_gain 与 best\_steps（即 $\arg\max_t S_t$）。
5. 若 gain 连续为负达到阈值（当前 3 次）且已有正收益，则提前停止。

后处理：

1. 候选按 gain、steps 排序。
2. 先做无重叠冲突消解。
3. 再把 candidate 方案与 greedy 方案分别估算总字节，取更小者。

### 相对原始方案的不同

原始 Tree+VB 更偏“可走多远走多远”，没有明确 best prefix 的收益截断。

### 提升机制

1. 显式抑制无效延伸。
2. 通过 candidate-vs-greedy 二选一降低误判风险。

---

## 4.5 阶段 E：局部精修（1-exchange 主线，2-exchange 关闭）

### 定义

阶段 E 目标是在低复杂度下修复冲突造成的局部次优。

### 具体计算方式

1. 1-exchange（默认启用）：
   1. 找到一个未选候选路径 $a$。
   2. 若 $a$ 只与一条已选路径冲突，且 $gain(a)>gain(old)$，则替换。
2. 2-exchange（代码保留，默认关闭）：
   1. 两条未选候选联合替换两条已选路径。
   2. 需满足净收益提升、候选互不重叠等约束。

### 相对原始方案的不同

原始 Tree+VB 没有交换型局部修复步骤。

### 提升机制

1. 1-exchange 以很小计算代价获得稳定增益。
2. 2-exchange 在本项目 A/B 中无额外 BPE 收益且带来速度损失，因此不进默认主线。

---

## 5. 与原始 Tree+VB 的核心差异总表

1. 输入构建：原始全量驻留，SACT 为双遍流式 CSR。
2. 节点 ID：原始不重排，SACT 做码长感知重编号。
3. 父森林：原始 first-fit，SACT 为收益驱动+阈值覆盖。
4. 路径决策：原始偏最长可达，SACT 用边际收益最优前缀。
5. 行内修复：原始无交换，SACT 有 1-exchange（2-exchange 保留关闭）。

---

## 6. 复杂性与可扩展性定义

在当前参数下：

1. 阶段 A 约为 $O(E)+O(\sum_v d_v\log d_v)$。
2. 阶段 B 约为 $O(E)+O(|V|\log|V|)$。
3. 阶段 C 约为 $O(E\cdot K)$，$K$ 为候选上限常数。
4. 阶段 D/E 在每行近似线性，受 max\_depth 和候选规模约束。
5. 解码复杂度与原始同阶，语义不变。

---

## 7. NP-Hard 性质证明

### 定理

固定重编号与父森林后，行内最优片段选择子问题已是 NP-Hard；因此 SACT 联合优化问题 NP-Hard。

### 证明思路

1. 固定 $(\pi,y)$，每行只优化片段选择 $x$。
2. 该子问题等价于“从候选路径集合中选互不重叠子集，使总收益最大”。
3. 这与带权集合打包（Weighted Set Packing / MWSP）同构。
4. MWSP 是 NP-Hard，所以该子问题 NP-Hard。
5. 联合问题包含该子问题作为特例，因此也 NP-Hard。\(\blacksquare\)

---

## 8. 下界与近似保证

记最大路径长度上界为 $D$，定义：

1. $S_{OPT}$：固定 $(\pi,y)$ 下单行最优节约量。
2. $S_G$：按收益排序的贪心互斥选择节约量。
3. $S_{1x}$：在 $S_G$ 上做受限 1-exchange 后节约量。

### 定理

$$
S_{1x}\ge S_G\ge \frac{1}{D}S_{OPT}
$$

### 证明要点

1. 1-exchange 只在净收益为正时替换，故 $S_{1x}\ge S_G$。
2. 任一贪心选中片段最多会与最优解中 $D$ 条片段发生覆盖冲突。
3. 由贪心排序性质，可对冲突最优收益做 $D$ 倍摊销上界。
4. 于是 $S_{OPT}\le D\cdot S_G$，即 $S_G\ge \frac{1}{D}S_{OPT}$。\(\blacksquare\)

### 紧性说明

可构造比值逼近 $1/D$ 的反例，因此在无额外分布假设下，$1/D$ 是该贪心框架可统一保证的最大下界。

---

## 9. 当前终稿的实测提升总结

相对原始 Tree+VB，终稿主线（A+B+C+D+E 的 1-exchange）在 16 数据集上表现为：

1. BPE：better/equal/worse = 16/0/0。
2. 平均 Delta BPE：约 -1.896627。
3. 速度：最新复跑均值保持同量级并不劣，平均比约 1.019315。
4. 2-exchange：A/B 显示 BPE 无新增益且速度约 0.9568x，故默认关闭。

---

## 10. 最终定义结论

1. SACT 是“单算法内闭环优化”，不是多算法拼接。
2. 五阶段定义均已具备可计算实现，不再停留在概念层。
3. 理论上问题 NP-Hard，工程上采用可证明下界的近似框架。
4. 实证上在压缩率与速度之间达成了当前最优折中，形成稳定终稿基线。

### 10.1 最终汇报输出规范

1. 表格字段：`Data_MB`、`BPE`、`Ratio(%)`、`MEPS`、`Correct`。
2. 曲线图输出：
   1. `bpe_comparison.png`
   2. `ratio_comparison.png`
   3. `meps_comparison.png`
