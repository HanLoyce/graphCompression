### 1. Optimization Problem Formulation

为了从理论层面剖析基于参考树的图压缩算法与变长字节编码（Variable-Byte Encoding）的结合机制，我们将该启发式过程抽象为整数线性规划（Integer Linear Programming, ILP）模型。

设输入有向图或无向图为 $G = (V, E)$。对于任意节点 $v \in V$，其对应的去重有序邻接表表示为 $L_v \subset V$。
我们的目标是：(1) 构建一个全局参考森林 $Y$；(2) 将每个邻接表 $L_v$ 划分为若干个互不相交的序列片段（Segments）。片段 $s$ 既可以是基于树 $Y$ 的多节点路径（包含节点数 $k \ge 2$），也可以是独立单节点（$k = 1$）。片段集合必须精确覆盖图中的所有边，同时使总编码代价最小化。

**变量定义：**
* **全局树拓扑变量** $y_{u,w} \in \{0, 1\}$：若在全局森林中节点 $u$ 是节点 $w$ 的父节点，则为 1，否则为 0。
* **局部划分变量** $x_{s,v} \in \{0, 1\}$：设 $\mathcal{S}_v$ 为由 $L_v$ 中节点构成的所有合法片段的候选集。若在压缩 $L_v$ 时选择片段 $s$ 进行打包，则 $x_{s,v} = 1$，否则为 0。

**代价函数：**
定义 $\text{VByte}(x)$ 为将整数值 $x$ 进行变长字节编码后所占用的**字节长度**。对于任意片段 $s \in \mathcal{S}_v$，设其起点节点为 $bottom$，包含的节点数量为 $k$。若 $k \ge 2$，则该路径沿着树往上回溯的步数为 $k - 1$。片段 $s$ 的总编码字节代价 $C(s)$ 表示为：

$$
C(s) = 
\begin{cases} 
\text{VByte}(bottom \ll 1 \mid 0), & \text{if } k = 1 \text{ (Isolated Node)} \\
\text{VByte}(bottom \ll 1 \mid 1) + \text{VByte}(k-1), & \text{if } k \ge 2 \text{ (Multi-node Path)}
\end{cases}
$$

*(注：位运算 $\ll 1 \mid 1$ 与 $\ll 1 \mid 0$ 用于在最低位封装路径标识符。$\text{VByte}(k-1)$ 表示对数值 $k-1$ 进行编码所需的字节数，而非 $k-1$ 个字节。)*

**ILP 最优化模型：**
$$ \min_{Y, X} \sum_{v \in V} \sum_{s \in \mathcal{S}_v} C(s) \cdot x_{s,v} $$

**约束条件：**
1. **全局森林约束**：每个节点最多有一个父节点，且拓扑无环。
   $$ \sum_{u \in V} y_{u,w} \le 1, \quad \forall w \in V $$
2. **片段合法性约束**：若在 $L_v$ 中选用多节点片段 $s = \langle u_1, u_2, \dots, u_k \rangle$，则全局森林中必须存在对应的连通路径。
   $$ y_{u_{i+1}, u_i} \ge x_{s,v}, \quad \forall (u_i, u_{i+1}) \in s, \forall v \in V $$
3. **精确覆盖约束**：$L_v$ 中的每一个节点 $u$，必须被且仅被一个选中的片段覆盖。
   $$ \sum_{s \in \mathcal{S}_v : u \in s} x_{s,v} = 1, \quad \forall u \in L_v, \forall v \in V $$

---

### 2. NP-Hardness Proof

**Theorem 1**: 给定图 $G=(V,E)$，寻找最优的全局参考森林 $Y$ 及对应的局部片段精确覆盖 $X$ 以最小化 VByte 总编码代价的问题，是 NP-Hard 的。

**Proof**:
我们通过从经典的**带权最大集合打包问题 (Maximum Weight Set Packing, MWSP)** 规约来证明此定理。

假设全局参考森林 $Y$ 已作为先验条件固定，我们仅需在 $Y$ 上优化邻接表 $L_v$ 的片段划分。最小化总代价等价于最大化相较于基准存储（全单节点）所节约的字节数。
定义片段 $s$ 的空间节约收益为 $W(s)$：
* 若 $s$ 为单节点，$W(s) = 0$。
* 若 $s$ 为长度 $k \ge 2$ 的路径：
  $$ W(s) = \left( \sum_{u \in s} \text{VByte}(u \ll 1 \mid 0) \right) - C(s) $$

基于精确覆盖约束，我们需要从 $\mathcal{S}_v$ 中选出一个子集，使得片段之间互不相交，且总收益 $\sum W(s)$ 最大化。未被多节点路径覆盖的节点将强制作为单节点处理。
该子问题与 MWSP 完全同构：全集对应 $L_v$ 中的节点，候选子集族对应基于 $Y$ 的所有合法多节点路径，子集权重对应 $W(s)$，且满足互斥约束。由于 MWSP 是已知的 NP-Hard 问题，在给定树结构下的路径最优划分同样是 NP-Hard 的。因此，联合优化树拓扑 $Y$ 与路径划分 $X$ 的原问题必然属于 NP-Hard。 $\blacksquare$

---

### 3. Approximation Guarantee of the Greedy Algorithm

鉴于原问题为 NP-Hard，本算法采用 $O(E \log E)$ 的启发式策略求解。以下证明其路径提取阶段的近似比下界。令算法允许的最大路径长度（节点数）为 $D$。

**Theorem 2**: 针对邻接表的精确覆盖阶段，本算法提出的基于自底向上的倒序贪心路径提取策略（Bottom-up Greedy Path Extraction），对于最大化空间节约量 $S$，能够保证严格的近似比下界：
$$ S_{Greedy} \ge \frac{1}{D} \cdot S_{OPT} $$

**Proof**:
在路径提取阶段，算法对重排后的邻接表进行倒序扫描。当扫描到未被占用的节点时，贪心地沿父节点回溯以提取极大路径片段 $p_g$（长度限制为 $\le D$），并立即将 $p_g$ 上的节点标记为互斥状态。
该过程等价于求解受限于基数 $D$ 的极大独立集问题（$D$-Set Packing）。假设在未知最优解中存在产生最优节约量的路径集合 $\mathcal{P}_{opt}$。在最坏情况下，贪心算法选中的某条路径 $p_g$ 的 $D$ 个节点，可能恰好导致 $\mathcal{P}_{opt}$ 中 $D$ 条不同的最优路径因节点冲突而失效。
由于 VByte 代价节约量 $W(s)$ 与路径包含的节点数呈正相关，根据贪心选择的局部最优性，被破坏的任意一条最优路径的收益必然不大于当前驱使算法做出选择的路径收益 $W(p_g)$。
因此，单次贪心选择所导致的潜在最优收益损失，被严格限制在 $D \cdot W(p_g)$ 以内。对全局所有贪心选择路径求和，满足：
$$ D \cdot \sum W(p_g) \ge \sum W(p_{opt}) \implies S_{Greedy} \ge \frac{1}{D} \cdot S_{OPT} $$
**Remark**: 上述界限为最坏情况下的理论下界。在算法的预处理阶段，我们引入了全局频次降序重排。该策略使高频节点集中于树的浅层，从而将数据共现分布高度对齐。这在极大提升单次贪心收益 $W(p_g)$ 的同时，呈指数级降低了不同长路径之间的冲突概率。因此在真实无标度图（Scale-free Graphs）中，算法的实际近似比远高于 $1/D$ 并趋近于 1。 $\blacksquare$