# 图压缩方案对比与单算法根本优化路线记录

## 1. 本轮约束与目标

本轮方向明确如下：

1. 只保留树结构字典 + 自底向上路径提取 + VByte 的单算法主线。
2. 不采用多算法并行选择最优，不做组合器，不做拼接式框架。
3. 优化目标是从算法内部机制出发，提升压缩率并尽量保持解码速度。

统一优化目标：

$$
\min C_{total} = C_{dict} + \sum_{v \in V} C(v)
$$

其中 $C(v)$ 是第 $v$ 行邻接表指令流的真实字节开销，$C_{dict}$ 是字典与索引相关开销。

---

## 2. 不可改变的核心骨架

为了保持算法身份一致，下列机制视为硬约束：

1. 全局仍使用一维父节点森林，每个节点至多一个父节点。
2. 行内编码仍是自底向上回溯，输出 single/path 两类指令。
3. 解码端仍按 parent 链复原，不引入外部模型与额外数据结构依赖。

这意味着优化必须发生在同一条流水线内部，而不是切换到其他压缩范式。

---

## 3. 当前实验事实（2026-04-06，16 数据集）

基于当前 benchmark_results.csv 的统计：

1. 相对 Your Algorithm (Tree+VB) 的平均 BPE 变化：
   - Optimized Tree+VB：+1.106977（better/equal/worse = 5/0/11）
   - Optimized Tree+VB V2 (MWSP-lite)：+2.474834（4/0/12）
   - Optimized Tree+VB V3 (Adaptive+1Ex)：+2.922765（4/0/12）
2. 在四个树系版本中，Your Algorithm (Tree+VB) 夺得 11/16 的最优 BPE。
3. 速度方面，三个优化版平均 MEPS 比基线分别是 0.886、0.893、0.941，整体也未显著占优。

结论：问题不是“还不够复杂”，而是现有优化方向偏离了真实字节目标，破坏了原始算法已经形成的高价值链结构。

---

## 4. 退化的根因（单算法视角）

### 4.1 父边建立目标错配

现有建树规则更多依赖频次与局部启发式，未直接优化“未来可节省字节数”。
结果是父关系看起来合理，但对编码收益不一定最优。

### 4.2 路径延伸缺少边际收益刹车

自底向上延伸时，是否继续向上主要由结构可达性决定，而不是每一步的边际收益决定。
会出现“路径更长但净收益变差”的情况。

### 4.3 冲突修复能力与代价模型不闭环

V2/V3 引入候选与交换，但局部替换仍是近邻策略，缺少稳定的全行收益闭环，容易在高冲突行做出次优取舍。

### 4.4 编号策略没有和字节代价联动

VByte 成本由 ID 大小强烈决定，但当前流程对“哪些节点应该优先拿到短字节 ID”缺少明确优化。

### 4.5 大图内存组织仍偏重

GraphLoader 先攒全量边再建 CSR，在超大图上会出现较高峰值内存，不利于稳定扩展。

---

## 5. 根本优化方案（只保留单算法）

方案名：单算法闭环优化树压缩（Single-Algorithm Closed-loop Tree+VB，简称 SACT）

### 5.1 总体思路

只做一套算法，但把每个阶段都改成“对真实字节目标负责”：

1. 统计阶段输出可用于字节优化的充分信号。
2. 建树阶段按可复用收益选父边，而不是仅按频次。
3. 路径阶段按边际收益决定延伸与截断。
4. 冲突阶段用受限但稳定的局部精修，避免高代价全局搜索。

---

### 5.2 阶段 A：双遍流式统计（替代全量驻留）

第一遍只统计，不保留完整边集：

1. 节点频次 $f(u)$。
2. 节点作为潜在父节点的贡献统计 $p(u)$。
3. 行度分布与高冲突行候选标记。

第二遍分块构建与编码，块内完成排序、建树、路径提取、落盘。

目标：

1. 峰值内存从与总边数同阶，降到与块大小同阶。
2. 为后续收益建模提供稳定统计量。

---

### 5.3 阶段 B：码长感知重编号（同算法内核，不是外部算法）

对节点进行一次全局重编号，使高贡献节点优先落入短 VByte 区间。

贡献分数定义：

$$
score(u) = \alpha f(u) + \beta p(u) + \gamma b(u)
$$

其中 $b(u)$ 是作为路径底节点的预估频次，$\alpha,\beta,\gamma$ 由数据集统计标定。

按分数降序分配新 ID，可近似优化：

$$
\min \sum_u score(u) \cdot \ell(id(u))
$$

其中 $\ell(\cdot)$ 为 VByte 字节长度函数。

---

### 5.4 阶段 C：收益驱动父森林构建

对每个子节点 $c$ 的候选父节点 $p$ 定义父边收益：

$$
G(p \to c) = R(p,c) - \lambda_1 \Delta_{id}(c) - \lambda_2 Risk(p,c) - \lambda_3 Depth(p)
$$

解释：

1. $R(p,c)$：该父边在历史行中可复用的路径收益。
2. $\Delta_{id}(c)$：由 ID 字节长度导致的额外编码代价。
3. $Risk(p,c)$：该绑定引发冲突或断链的风险估计。
4. $Depth(p)$：过深树层导致回溯链脆弱的惩罚。

选择规则：

1. 每个子节点只接受正收益最高的父节点。
2. 若所有候选收益非正，则保持无父节点。

这一步仍满足“一维父节点森林”约束。

---

### 5.5 阶段 D：自底向上路径提取的边际收益截断

对每个底节点，从下往上逐步扩展路径，但每扩展一步都计算边际收益。

设扩展到第 $t$ 步时，累计收益为：

$$
S_t = Bytes_{single}(prefix_t) - Bytes_{path}(bottom,t)
$$

策略：

1. 记录使 $S_t$ 最大的最优前缀 $t^*$。
2. 当边际收益连续为负且超过阈值时提前停止。
3. 最终只输出最优前缀，而非盲目最长前缀。

这能直接抑制“长但不值钱”的路径。

---

### 5.6 阶段 E：受限局部精修（单算法内）

仅在高冲突行启用轻量修复：

1. 允许 1-exchange 或 2-exchange 替换。
2. 替换条件必须满足净收益严格增加。
3. 迭代次数设上限，保证复杂度可控。

这不是多算法拼接，而是同一算法的局部求精步骤。

---

## 6. 复杂度与工程可行性

在保持单算法的前提下，目标复杂度：

1. 统计与重排：$O(E \log d_{avg})$。
2. 父边打分与选择：$O(E \cdot k)$，$k$ 为每子节点候选父数量上限。
3. 路径提取与局部精修：近似线性于行长，且仅对高冲突行做附加计算。
4. 解码复杂度保持与当前同阶，不改变解码语义。

---

## 7. 分阶段落地计划（下一步编码依据）

### Phase A：打通单算法闭环

1. 接入双遍统计与块处理框架。
2. 增加码长感知重编号。
3. 引入父边收益打分并替换先到先得绑定。

### Phase B：增强路径决策

1. 实装边际收益截断与最优前缀保留。
2. 加入高冲突行的受限 1/2-exchange 精修。

### Phase C：大图稳态化

1. 编码流边处理边落盘。
2. 行偏移改为稀疏锚点索引，控制索引内存。

---

## 8. 验收指标（单算法版本）

1. 压缩率：Data BPE 与 Total BPE 的均值、中位数、P90。
2. 稳定性：在 16 个数据集上相对当前 Your Algorithm (Tree+VB) 的 better/equal/worse。
3. 速度：编码 MEPS 与解码 MEPS 的变化。
4. 内存：峰值 RSS 与平均工作集。
5. 正确性：解码边数与原图边数严格一致。

---

## 9. 当前决策

1. 终止“多算法并行择优”方向，不再推进任何组合式路线。
2. 继续沿树结构字典 + 自底向上读取主线，做目标一致的内部优化。
3. 下一步按 Phase A 开始编码实现，并在每个阶段复跑全量数据集验证。

---

## 10. 下一步迭代记录（2026-04-07）

### 10.1 图中看不到 Tree+VB 的原因

不是算法缺失，而是可视化遮挡：

1. Tree+VB 与 Tree+VB SACT 的 BPE/MEPS 曲线高度重合。
2. 原绘图顺序里 SACT 后绘制，视觉上覆盖了 Tree+VB。

已修复 `plot_results.py`：

1. 调整绘制顺序（SACT 先画，Tree+VB 后画）。
2. Tree+VB 改为虚线+空心标记，提高重合场景下可见性。

### 10.2 SACT 与设计文档的落实状态

已落实（部分）：

1. 阶段 D：边际收益截断与最优前缀选择（best gain / best steps）。
2. 候选路径冲突消解（按 gain 排序后无冲突选择）。
3. 行内双策略择优（candidate 与原始 greedy 按真实字节估算选更优）。
4. 阶段 E（部分）：受限 1-exchange 精修（单冲突且收益严格提升时替换）。

未落实（关键缺口）：

1. 阶段 A：双遍流式统计与分块编码。
2. 阶段 B：码长感知重编号。
3. 阶段 C：收益驱动父森林（当前仍是 first-fit 父绑定）。
4. 阶段 E（完整）：2-exchange 及更系统的局部闭环精修。

### 10.3 本次复跑结果（16 数据集）

相对 `Your Algorithm (Tree+VB)`：

1. `Your Algorithm (Tree+VB SACT)` 的 BPE：better/equal/worse = 15/1/0。
2. 平均 `Delta BPE = BPE(SACT) - BPE(Tree+VB) = -0.004591`。
3. 速度对比（MEPS）：faster/equal/slower = 8/0/8，平均比值约 `1.000029`。

### 10.4 为什么提升仍然微乎其微

根因是“优化杠杆还没切到高增益层”：

1. 父森林仍是 first-fit，尚未进入收益驱动选父，导致可复用链结构与原始版高度一致。
2. 码长感知重编号未做，VByte 主导成本的关键变量（ID 码长分布）没有改变。
3. 受限 1-exchange 属于行内微调，只能修复局部冲突，难以带来全局量级收益。
4. 当前 SACT 已采用“candidate vs greedy 逐行择优”，这会抑制退化，但也意味着可进一步压缩的剩余空间主要在上游（父边与编号）。

### 10.5 下一步执行建议（单算法内）

1. 优先实现阶段 C：将父节点绑定从 first-fit 改为收益打分选择。
2. 其次实现阶段 B：码长感知重编号，把高贡献节点压入短 VByte 区间。
3. 在阶段 C/B 完成后，再扩展阶段 E 到 2-exchange，避免在现有上游结构下做低收益微调。

---

## 11. 阶段 C 落地迭代记录（2026-04-07，最终）

### 11.1 本次实现内容

在保持单算法约束下，完成了“保守收益驱动选父”的落地：

1. 先按原有 first-fit 生成默认父关系，作为稳定基线。
2. 统计每个子节点的 Top-K 父候选收益分数（局部路径收益 + 稳定性项 - 轻量码长惩罚）。
3. 仅当候选满足置信阈值（支持度与分数阈值）时，覆盖 first-fit 父边。
4. 保留行内候选路径冲突消解与受限 1-exchange 精修。

这保证了“高收益时替换、低置信时回退”，避免小图或噪声行上的激进退化。

### 11.2 与设计方案的一致性更新

一致性提升点：

1. 阶段 C（收益驱动父森林）已从“概念”进入“可运行实现（保守版）”。
2. 阶段 D（边际收益截断）与阶段 E（受限 1-exchange）仍保留并可工作。

仍未完成：

1. 阶段 A 双遍流式统计与分块编码。
2. 阶段 B 码长感知重编号。
3. 阶段 E 的 2-exchange 完整闭环。

### 11.3 本次全量复跑结果（16 数据集）

相对 `Your Algorithm (Tree+VB)`：

1. `Your Algorithm (Tree+VB SACT)` 的 BPE：better/equal/worse = 16/0/0。
2. 平均 `Delta BPE = BPE(SACT) - BPE(Tree+VB) = -1.233148`。
3. 速度对比（MEPS）：faster/equal/slower = 5/0/11。
4. 平均 `MEPS(SACT)/MEPS(Tree+VB) = 1.007412`。

说明：当前实现在压缩率上取得了显著而稳定的全数据集提升，速度整体与基线同量级（均值略高）。

### 11.4 为什么这次提升明显

1. 先前提升微小的主要原因是“父森林不变”，收益空间被锁死在行内微调。
2. 本次改为“收益驱动覆盖 first-fit”，直接改变了上游可复用链结构。
3. 保守阈值与回退机制减少了劣化覆盖，避免了阶段 C 激进版本中的局部反噪声失稳。

### 11.5 下一步建议

1. 优先推进阶段 B（码长感知重编号），预计可继续优化 VByte 主导成本。
2. 在阶段 B 完成后评估阶段 E 的 2-exchange，避免过早引入高复杂度局部搜索。
3. 并行准备阶段 A（双遍流式统计）以改善超大图内存稳态。

---

## 12. 阶段 B 落地迭代记录（2026-04-07）

### 12.1 本次实现内容

在 `Tree+VB SACT` 分支中实装了码长感知重编号（不改变 Tree+VB 基线）：

1. 基于当前图统计 `base_freq`（节点出现频次）。
2. 在每一行按频次优先排序后，累计父候选贡献 `parent_potential` 与底节点贡献 `bottom_potential`。
3. 按分数

$$
score(u) = 1.00 \cdot base\_freq(u) + 0.75 \cdot parent\_potential(u) + 0.50 \cdot bottom\_potential(u)
$$

对节点全局排序并重映射到新 ID（高贡献节点优先获得短 VByte 区间）。
4. 用重编号后的 ID 继续执行既有阶段 C/D/E 流程（收益驱动选父 + 行内冲突消解 + 受限 1-exchange）。

### 12.2 本次全量复跑结果（16 数据集）

相对 `Your Algorithm (Tree+VB)`：

1. `Your Algorithm (Tree+VB SACT)` 的 BPE：better/equal/worse = 16/0/0。
2. 平均 `Delta BPE = BPE(SACT) - BPE(Tree+VB) = -1.896627`。
3. 速度对比（MEPS）：faster/equal/slower = 6/0/10。
4. 平均 `MEPS(SACT)/MEPS(Tree+VB) = 0.981721`。

补充：

1. 最优提升数据集：`soc-LiveJournal1`，`Delta BPE = -3.781000`。
2. 最小提升数据集：`web-uk-2005`，`Delta BPE = -0.127045`。

### 12.3 阶段性结论

1. 阶段 B 对压缩率有明显正向增益，且在 16/16 数据集上保持稳定改进。
2. 速度端出现轻微均值回落（约 1.8%），说明重编号后路径结构更偏压缩率最优而非解码局部性最优。
3. 下一步建议优先进入阶段 A（双遍流式统计与分块），一方面改善大图内存稳态，另一方面为阶段 B/C 的参数做更细粒度分组标定。

---

## 13. 阶段 A 落地迭代记录（2026-04-07）

### 13.1 本次实现内容

完成了 `GraphLoader` 的双遍流式改造（保留算法语义不变）：

1. 第一遍仅统计 `num_nodes` 与每行度数，不保留全量边列表。
2. 按度数前缀和一次性分配 CSR 空间。
3. 第二遍流式写入 CSR 的 `col_idx`。
4. 对每行做排序去重并压紧 CSR，得到最终 `row_ptr/col_idx`。

该实现替代了原先“全量边向量 + 每行 vector 邻接”的高峰值内存构建方式。

### 13.2 本次全量复跑结果（16 数据集）

相对 `Your Algorithm (Tree+VB)`：

1. `Your Algorithm (Tree+VB SACT)` 的 BPE：better/equal/worse = 16/0/0。
2. 平均 `Delta BPE = BPE(SACT) - BPE(Tree+VB) = -1.896627`。
3. 速度对比（MEPS）：faster/equal/slower = 9/0/7。
4. 平均 `MEPS(SACT)/MEPS(Tree+VB) = 1.062311`。

补充：

1. 最大压缩收益数据集：`soc-LiveJournal1`，`Delta BPE = -3.781000`。
2. 最小压缩收益数据集：`web-uk-2005`，`Delta BPE = -0.127045`。

### 13.3 阶段性结论

1. 阶段 A 改造未改变压缩率结论，说明语义一致性良好。
2. 在当前测试中，速度均值从上一轮约 `0.981721` 提升到 `1.062311`。
3. 单算法路线下，当前版本已形成“阶段 B + C + D + E(1-exchange) + A(双遍加载)”的稳定闭环，可作为后续阶段 E(2-exchange) 的新基线。

---

## 14. 最终决策与迭代冻结方案（2026-04-07）

### 14.1 SACT 设计是否已全部实现

结论：主链路已打通，但“E 的 2-exchange”不作为默认启用项。

已完成并生效：

1. 阶段 A：双遍流式加载 + CSR 压紧。
2. 阶段 B：码长感知重编号。
3. 阶段 C：收益驱动父森林（保守阈值覆盖 first-fit）。
4. 阶段 D：边际收益截断与最优前缀。
5. 阶段 E（部分）：受限 1-exchange。

未默认启用：

1. 阶段 E 的 2-exchange（已做 A/B 验证，不纳入默认配置）。

### 14.2 2-exchange 的必要性与收益判断

本轮做了受限 2-exchange 的实现与全量 A/B：

1. 压缩率：`SACT(BPE)` 相比“仅 1-exchange 版本”无改进（16/16 数据集等值）。
2. 速度：2-exchange 版本在 A/B 中出现明显回落（平均约 `0.9568x`，即约 `-4.3%`）。
3. 结论：在当前算法形态和数据分布下，2-exchange 不是高 ROI 优化，默认关闭更优。

### 14.3 最终冻结版本（建议作为后续基线）

最终版本定义：

1. 保留 A + B + C + D + E(1-exchange)。
2. 2-exchange 逻辑保留在代码中但默认关闭，仅用于后续定向实验。
3. 以该版本作为后续迭代与论文/报告的“稳定基线版本”。

最终版本全量结果（相对 `Your Algorithm (Tree+VB)`）：

1. BPE better/equal/worse = 16/0/0。
2. 平均 `Delta BPE = -1.896627`。
3. 速度均值保持同量级并整体不劣（最新复跑平均比值约 `1.019315`）。

### 14.4 后续迭代优先级（最终）

1. 优先做阶段 B/C 参数自适应（按图规模与度分布调参），目标是稳住不同图上的速度波动。
2. 阶段 A 的分块落盘与内存观测（RSS 采样）补齐工程指标。
3. 2-exchange 仅在“高冲突子集数据集”离线实验，不进入主线默认配置。

---

## 15. 理论完备补全（终稿）

本节按“三大问题”同类范式，给出 SACT 主线的形式化问题建模、求解流程、优化目标、复杂性性质与可证明下界。

### 15.1 统一问题建模（SACT-ILP 抽象）

设输入图为 $G=(V,E)$，每一行邻接表为 $L_v \subseteq V$（已去重）。
SACT 联合优化的核心对象为三层变量：

1. **重编号映射**：$\pi: V \to \{0,1,\dots,|V|-1\}$，为双射。
2. **父森林拓扑**：$y_{u,w}\in\{0,1\}$，若节点 $u$ 是节点 $w$ 的父节点则为 1。
3. **行内片段选择**：$x_{s,v}\in\{0,1\}$，其中 $s\in\mathcal{S}_v$ 是 $L_v$ 的候选片段（单点或沿父链的路径片段）。

定义 VByte 字节长度函数 $\ell(\cdot)$，片段 $s$ 的编码代价：

$$
C_\pi(s)=
\begin{cases}
\ell((\pi(bottom)\ll 1)\mid 0), & |s|=1 \\
\ell((\pi(bottom)\ll 1)\mid 1)+\ell(|s|-1), & |s|\ge 2
\end{cases}
$$

总目标：

$$
\min_{\pi,y,x}\; C_{total}(\pi,y,x)
= C_{dict}(\pi,y)+\sum_{v\in V}\sum_{s\in\mathcal{S}_v} C_\pi(s)\,x_{s,v}
$$

约束：

1. **重编号双射约束**：$\pi$ 为一一映射。
2. **父森林约束**：每个节点至多一个父节点且无环。
   $$
   \sum_{u\in V}y_{u,w}\le 1,\;\forall w\in V
   $$
3. **片段合法性约束**：若选路径片段 $s=\langle u_0,u_1,\dots,u_t\rangle$，则父边必须存在。
   $$
   y_{u_i,u_{i-1}}\ge x_{s,v},\;\forall i\in[1,t]
   $$
4. **精确覆盖约束**：每行每个邻接点被且仅被一个片段覆盖。
   $$
   \sum_{s\in\mathcal{S}_v: u\in s}x_{s,v}=1,\;\forall u\in L_v
   $$

该抽象恰好对应当前实现中的 A/B/C/D/E(1-exchange) 流程。

### 15.2 具体开展步骤（与实现对齐）

SACT 终稿流水线为：

1. **步骤 A（双遍流式加载）**：第一遍统计度数，第二遍写入 CSR，再排序去重压紧。
2. **步骤 B（码长感知重编号）**：按

$$
score(u)=\alpha f(u)+\beta p(u)+\gamma b(u)
$$

排序并映射短 ID 给高贡献节点。
3. **步骤 C（收益驱动选父）**：在 first-fit 基线上做高置信候选覆盖。
4. **步骤 D（边际收益截断）**：对每个底节点保留收益最优前缀路径。
5. **步骤 E（局部精修）**：候选冲突消解 + 受限 1-exchange（2-exchange 保留但默认关闭）。
6. **编码/解码一致性校验与全量复跑**：输出 BPE、MEPS、Correct。

### 15.3 优化目标与定义

定义基线（全 single）在固定 $(\pi,y)$ 下的总代价为：

$$
C_{single}(\pi)=\sum_{(v,u)\in E}\ell((\pi(u)\ll 1)\mid 0)
$$

定义片段节约收益：

$$
W_\pi(s)=\sum_{u\in s}\ell((\pi(u)\ll 1)\mid 0)-C_\pi(s)
$$

则任意可行解的总节约量为：

$$
S(\pi,y,x)=\sum_{v\in V}\sum_{s\in\mathcal{S}_v}W_\pi(s)x_{s,v}
$$

并有：

$$
C_{total}(\pi,y,x)=C_{single}(\pi)-S(\pi,y,x)+C_{dict}(\pi,y)
$$

因此压缩问题等价于在可行域内**最大化总节约量**。

### 15.4 相关算法与复杂度性质

当前终稿对应的求解器是分层启发式：

1. A：双遍 CSR 构建，时间 $O(E)+O(\sum_v d_v\log d_v)$，空间峰值由全边驻留降为 CSR 同阶。
2. B：重编号排序，时间 $O(|V|\log |V|)$。
3. C：Top-K 候选父打分，时间 $O(E\cdot K)$（$K$ 为常数）。
4. D/E：每行近似线性，最坏受 `max_depth` 与候选数量上界约束。

该复杂度满足工程可运行性，且与当前 16 数据集全量验证一致。

### 15.5 NP-Hard 性质证明

**定理 1（NP-Hard）**：SACT 的联合优化问题是 NP-Hard。

**证明思路**：

1. 固定重编号 $\pi$ 与父森林 $y$，仅优化某一行的片段选择 $x$。
2. 该子问题等价于在候选路径集合上选择互不相交子集使总收益 $\sum W_\pi(s)$ 最大。
3. 这与带权集合打包（Weighted Set Packing / MWSP）同构，而 MWSP 是 NP-Hard。
4. 固定子问题已 NP-Hard，则包含其作为特例的联合问题 $(\pi,y,x)$ 也必为 NP-Hard。\(\blacksquare\)

这解释了为什么 SACT 采用“收益驱动 + 局部精修”的近似求解框架，而非全局精确求解。

### 15.6 最终最大下界分析与证明

记路径最大节点数上界为 $D$（实现中由 `max_depth` 给出上界），在固定 $(\pi,y)$ 的单行子问题上，令：

1. $S_{OPT}$：最优节约量。
2. $S_G$：按收益降序的贪心互斥选择节约量。
3. $S_{1x}$：在 $S_G$ 基础上做受限 1-exchange 后的节约量。

**定理 2（可保证下界）**：

$$
S_{1x}\ge S_G\ge \frac{1}{D}S_{OPT}
$$

**证明**：

1. $1$-exchange 仅在净收益严格增加时替换，故 $S_{1x}\ge S_G$。
2. 对贪心解中任一已选片段 $g$（大小至多 $D$），最优解中与其冲突的片段数至多 $D$（最优解片段互不重叠）。
3. 由贪心“先选高收益”性质，可将与 $g$ 冲突的最优片段收益总和按至多 $D\cdot W(g)$ 计费。
4. 对所有贪心片段求和得：

$$
S_{OPT}\le D\cdot S_G
\Rightarrow
S_G\ge \frac{1}{D}S_{OPT}
$$

再结合第 1 步即得结论。\(\blacksquare\)

**下界紧性（为何称“最大下界”）**：

可构造反例使贪心比值逼近 $1/D$：一条大收益片段与 $D$ 条两两不冲突的小片段全部冲突，且大片段收益仅略高于任一小片段。贪心先取大片段后无法再取其余，最优则取 $D$ 条小片段，总比值可逼近 $1/D$。因此在无额外分布假设下，$1/D$ 是该类贪心框架可统一保证的**最大全局下界**。

### 15.7 终稿理论结论

1. SACT 已具备从目标函数到工程实现的一致理论闭环。
2. 问题本质 NP-Hard，启发式与局部搜索是合理且必要的。
3. 当前默认版本（A+B+C+D+E(1-exchange), 2-exchange 关闭）在理论下界可保证、工程效率与实测收益之间取得最优折中。


